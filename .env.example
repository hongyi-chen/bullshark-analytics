# Bullsharks server endpoint that returns activities as JSON
# Must include the full path (currently `/read`)
SERVER_ACTIVITIES_URL=https://bullsharks-server-288102886042.us-central1.run.app/read

# Optional: used for generating absolute URLs in metadata/social cards.
# e.g. http://localhost:3000 (dev) or https://your-app.vercel.app (prod)
APP_BASE_URL=http://localhost:3000
